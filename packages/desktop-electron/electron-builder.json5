{
  appId: 'ai.coro.desktop',
  productName: 'Coro',
  artifactName: '${productName}-${version}-${arch}.${ext}',
  npmRebuild: false,
  files: [
    'dist/main.js',
    'dist/main.js.map',
    'dist/preload.js',
    'dist/preload.js.map',
    'package.json',
  ],
  extraResources: [
    {
      from: 'dist/resources/coro',
      to: 'coro',
    },
  ],
  directories: {
    output: 'release',
  },
  publish: [
    {
      provider: 'github',
      owner: 'Coro-ai-framework',
      repo: 'coro-release',
      releaseType: 'release',
      publishAutoUpdate: true,
    },
  ],
  afterSign: 'scripts/verify-mac-signing.mjs',
  mac: {
    category: 'public.app-category.developer-tools',
    icon: 'dist/icon.icns',
    hardenedRuntime: true,
    gatekeeperAssess: false,
    entitlements: 'build/entitlements.mac.plist',
    entitlementsInherit: 'build/entitlements.mac.plist',
    target: [
      {
        target: 'dmg',
        arch: ['arm64'],
      },
      {
        target: 'zip',
        arch: ['arm64'],
      },
    ],
  },
  win: {
    icon: 'dist/icon.ico',
    // CSC_LINK / CSC_KEY_PASSWORD from CI sign the NSIS installer when
    // WINDOWS_SIGNING_CERT_B64 secrets are configured (electron-builder default).
    target: [
      {
        target: 'nsis',
        arch: ['x64'],
      },
    ],
  },
  nsis: {
    oneClick: true,
    perMachine: false,
    allowToChangeInstallationDirectory: false,
  },
}