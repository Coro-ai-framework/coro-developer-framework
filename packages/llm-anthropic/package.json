{
  "name": "@coro/llm-anthropic",
  "version": "0.1.0",
  "description": "Anthropic Claude phase executor for Coro — wraps the Claude Agent SDK and exposes it through the PhaseExecutorRuntime contract.",
  "private": true,
  "license": "UNLICENSED",
  "type": "commonjs",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "README.md"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.2.92",
    "zod": "^4.3.6"
  },
  "peerDependencies": {
    "@coro/plugin-sdk": "workspace:*",
    "pino": "^9.0.0"
  },
  "devDependencies": {
    "@coro/plugin-sdk": "workspace:*",
    "@types/node": "^22.13.14",
    "pino": "^9.6.0",
    "typescript": "^5.6.3",
    "vitest": "^3.2.4"
  }
}
