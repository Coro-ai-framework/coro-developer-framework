{
  "name": "coro",
  "version": "0.1.0",
  "description": "Coro — multi-tenant, plug-and-play AI agent platform",
  "license": "BUSL-1.1",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "scripts": {
    "build": "pnpm -r --if-present run build",
    "build:publishable": "pnpm --filter @coro/cloud-protocol --filter @coro/intelligence-base --filter @coro/plugin-sdk --filter @coro/dashboard --filter @coro/llm-anthropic --filter @coro/llm-openai --filter @coro/plugin-gitlab --filter @coro/runner run build",
    "prepare:runner-npm": "node scripts/prepare-runner-npm-publish.mjs",
    "typecheck": "pnpm -r --if-present run typecheck",
    "test": "pnpm -r --if-present run test",
    "test:runner": "pnpm --filter @coro/runner test",
    "verify:npm-pack": "node scripts/verify-npm-pack.mjs",
    "start": "pnpm --filter @coro/runner start",
    "dev": "pnpm --filter @coro/runner dev",
    "dev:runner": "pnpm --filter @coro/runner dev",
    "dev:cloud": "pnpm --filter @coro/runner dev:cloud",
    "dev:dashboard": "pnpm --filter @coro/dashboard dev",
    "dev:landing": "pnpm --filter @coro/landing dev",
    "build:landing": "pnpm --filter @coro/landing build",
    "clean": "pnpm -r exec rm -rf dist node_modules .turbo"
  },
  "devDependencies": {
    "typescript": "^5.8.2"
  }
}
